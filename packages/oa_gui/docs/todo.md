Form with required field check when creating endpoint:
- Wenn adding an input, user must specify operationId, method, path
- We may need to modify "AddItem" behavior on List.

Tag assignment when endpoint is created:
- Tags are created separately (top-right). Endpoints can be assigned a tag. Therefore, we must implement a dropdown based on the existing tags.
- We need a sophisticated state management before implementing this feature. 

Selection of methods when endpoint is created